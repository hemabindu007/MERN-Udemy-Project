import type { CSSProperties } from 'react';

const styles: Record<string, CSSProperties> = {
    sider :{
        background:'#f0f2f5'
    },
    content:{
       margin:'2rem'
    }
}

export default styles;